# Legacy pip compatibility. The canonical dependency list lives in pyproject.toml.
accelerate
blobfile
datasets
httpx[http2]
mcp[cli]
megatron-bridge @ ./external/Megatron-Bridge ; sys_platform == "linux"
memray
nvidia-modelopt>=0.37.0 ; sys_platform == "linux"
omegaconf
pillow
pylatexenc
pyyaml
ray[default]
ring-flash-attn ; sys_platform == "linux"
sglang ; sys_platform == "linux"
sglang-router>=0.2.3 ; sys_platform == "linux"
tensorboard
torch>=2.0 ; sys_platform == "linux"
transformers
wandb
liger-kernel ; sys_platform == "linux"
